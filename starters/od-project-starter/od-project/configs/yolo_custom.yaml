task: object_detection
seed: 42
output_dir: outputs

model:
  weights: yolov8n.pt

data:
  yaml: data/data.yaml
  imgsz: 640
  batch: 16

train:
  epochs: 50
  lr0: 0.01
  weight_decay: 0.0005
  patience: 20

device: auto
